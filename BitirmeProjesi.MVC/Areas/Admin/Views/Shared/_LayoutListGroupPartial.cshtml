@model BitirmeProjesi.Entities.Dtos.BookSerieMovieDto

@*@{
    string active = string.Empty;
    if (TempData["Active"] != null)
    {
        active = TempData["Active"].ToString();
    }
}*@


<div class="col-lg-3">

    <h4 class="my-4 text-info h-auto w-auto">E-Hobilerim </h4>
    <div class="list-group ">
        @foreach (var categories in Model.Categories)
        {
            <a asp-area="Admin" asp-controller="Home" asp-action="Index" asp-route-id="@categories.Id"  class="list-group-item text-info @*@(active=="film"?"active":""*@)">@categories.Name</a>

        }


    </div>
</div>
